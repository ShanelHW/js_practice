const obj = {
    contents: ["bits", "pieces", "springs", "sprockets"]
}

for(let i = 0; i<= obj.contents.length; i++){
    console.log (`This object has ${obj.contents[i]}`)
}

const store = {
    storename: "ToysArentUs",
    address: 123 Main St,
    toy : {
        name: `Serf`, 
        type:`ball`, 
        color: `red`}
}

const corporation = {
    storename: "BlicksSportingGoods",
   const needRestock= {
    console.log(`There are ${corporation.items[0].quantity} `)
        store.push(items)
    },
    address: 987 Center Ln,
    items : {
        name: `Nalding`, 
        type:`racquet`, 
        color: `pink`,
        price:``,
        description: {
            hasAluminum: true,
            hasWood: false,
            hasNylonStrings: true,
        },
        quantity: 500,
        }
        {
            name: `Nalding`, 
            type:`ball`, 
            color: `red/white/blue`,
            price:``,
            description: {
                hasAluminum: true,
                hasWood: false,
                hasNylonStrings: true,
            },
            quantity: 500,
        }
}